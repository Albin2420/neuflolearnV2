import 'package:neuflo_learn/src/domain/repositories/profile/profile_repo.dart';

class ProfileRepoImpl extends ProfileRepo {
  @override
  Future fetchweekGrowth({required int studentId}) {
    try {
      throw UnimplementedError();
    } catch (e) {
      throw UnimplementedError();
    }
  }
}
